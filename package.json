{
  "name": "filtre-jeux",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Extension de filtrage des sites de jeux pour les salles informatiques d'un college",
  "license": "MIT",
  "scripts": {
    "build:lists": "node tools/build-lists.mjs",
    "report:diff": "node tools/report-diff.mjs",
    "lint": "web-ext lint --source-dir=extension --self-hosted",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run build:lists -- --quiet && npm run lint && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "web-ext": "^10.6.0"
  }
}
